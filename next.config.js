/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            '64.media.tumblr.com',
        ],
        unoptimized: true,
    }
}

module.exports = nextConfig
